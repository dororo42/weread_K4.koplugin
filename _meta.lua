-- Plugin metadata, loaded by KOReader at plugin-discovery time (before the
-- full plugin module is required). Keep this file dependency-free: do NOT
-- require weread modules here, otherwise the discovery phase pulls in extra
-- modules. Description text is plain (English); localization happens in the
-- plugin UI itself, not in _meta.
return {
    fullname = "WeRead",
    description = "Read WeRead books in KOReader, cache chapters, and sync reading progress.",
    version = "0.6.0-k4-v5.0",
}
